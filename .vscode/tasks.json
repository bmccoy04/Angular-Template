{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Angular-Template.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}